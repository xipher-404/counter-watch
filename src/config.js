// Cloud-vision endpoint (Supabase edge function) that turns a photo of a screen
// into a list of heroes per team. Filled in once the function is deployed
// (Phase B). While null, the "Add by photo" button explains it isn't set up yet
// and manual entry is used instead.
export const VISION_ENDPOINT = null;
export const VISION_ANON_KEY = null;
